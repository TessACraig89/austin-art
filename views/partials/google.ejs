
	  <div>
      <% if (user) { %>
        <h2> Below is the data sent by google</h2>
        <pre>
          <% if (user.google) {
            user.google
          } else { %>
            No user.google
          <% } %>
        </pre>
        <a href="/login">Logout</a>
      <% } else { %>
        <a href="/auth/google">Login with Google</a>
      <% } %>
	  </div>
